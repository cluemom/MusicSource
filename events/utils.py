# import facebook

def fetch_facebook_events(access_token, page_id):
    pass
    # todo:finish this
    # graph = facebook.GraphAPI(access_token)
    # events = graph.get_connections(page_id, 'events')
    # return events.get('data', [])
